<% provide(:title, '実験募集フォーム') %>
<h2><%= yield(:title) %></h2>

<%= render partial: 'form',
           locals: { form_for_url: experiments_path, experiment: @experiment, places: @places, submit_label: '募集する', type: 'all', show_errors: true } %>
